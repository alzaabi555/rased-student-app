import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alzaabi.rased.student',
  appName: 'Rased Student',
  webDir: 'dist'
};

export default config;
